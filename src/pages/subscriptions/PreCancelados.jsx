import{useEffect,useState}from'react';import{PageHeader}from'../../components/ui/PageHeader';import{subscriptionsService}from'../../services/subscriptions.service';export default function PreCancelados(){const[items,setItems]=useState([]);useEffect(()=>{subscriptionsService.getPipeline({stage:'pre_cancelled'}).then(r=>setItems(r.data.data?.data||[]))},[]);return <div><PageHeader title="Pré-Cancelados" subtitle="Pipeline real"/>{items.map(x=><div className="card mb-2" key={x.id}>{x.client?.name} · {x.reason}</div>)}</div>}
