import{useEffect,useState}from'react';import{PageHeader}from'../../components/ui/PageHeader';import{subscriptionsService}from'../../services/subscriptions.service';export default function ContratosAtivos(){const[items,setItems]=useState([]);useEffect(()=>{subscriptionsService.getContracts({status:'active'}).then(r=>setItems(r.data.data?.data||[]))},[]);return <div><PageHeader title="Contratos Ativos" subtitle="Assinaturas reais"/>{items.map(x=><div className="card mb-2" key={x.id}>{x.client?.name} · {x.plan?.name} · R${x.price}</div>)}</div>}
