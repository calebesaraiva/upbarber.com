import{useEffect,useState}from'react';import{PageHeader}from'../../components/ui/PageHeader';import{subscriptionsService}from'../../services/subscriptions.service';export default function PreAprovados(){const[items,setItems]=useState([]);useEffect(()=>{subscriptionsService.getPipeline({stage:'approved'}).then(r=>setItems(r.data.data?.data||[]))},[]);return <div><PageHeader title="Clientes Pré-Aprovados" subtitle="Pipeline real"/>{items.map(x=><div className="card mb-2" key={x.id}>{x.client?.name} · {x.plan?.name}</div>)}</div>}
