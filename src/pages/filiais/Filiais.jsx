import { useEffect,useState } from 'react';import{PageHeader}from'../../components/ui/PageHeader';import{branchesService}from'../../services/branches.service';
export default function Filiais(){const[items,setItems]=useState([]);useEffect(()=>{branchesService.list().then(r=>setItems(r.data.data||[]))},[]);return <div><PageHeader title="Filiais" subtitle="Unidades cadastradas na API"/><div className="grid md:grid-cols-2 gap-4">{items.map(x=><div className="card" key={x.id}><p className="text-white font-bold">{x.name}{x.isMain?' · Matriz':''}</p><p className="text-xs text-gray-500">{x.address} · {x.city}/{x.state}</p><p className="text-sm text-gold mt-3">{x.barberCount} profissionais · {x.clientCount} clientes · R${x.monthRevenue}</p></div>)}</div></div>}
