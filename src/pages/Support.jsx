import { useEffect, useState } from 'react';
import { Send } from 'lucide-react';
import { PageHeader } from '../components/ui/PageHeader';
import { supportService } from '../services/support.service';
import { useApp } from '../context/AppContext';

export default function Support(){const{addToast}=useApp();const[items,setItems]=useState([]);const[form,setForm]=useState({subject:'',body:'',priority:'medium'});const load=()=>supportService.list().then(r=>setItems(r.data.data||[]));useEffect(load,[]);const send=async()=>{try{await supportService.create(form);setForm({subject:'',body:'',priority:'medium'});await load();addToast('Chamado criado','success')}catch(e){addToast(e.response?.data?.error?.message||'Falha ao criar chamado','error')}};return <div><PageHeader title="Suporte" subtitle="Chamados reais enviados ao painel Master"/><div className="grid lg:grid-cols-2 gap-4"><div className="card space-y-3"><input className="input" placeholder="Assunto" value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/><textarea className="input" rows={6} placeholder="Descreva sua solicitação" value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/><select className="input" value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}><option value="low">Baixa</option><option value="medium">Média</option><option value="high">Alta</option></select><button className="btn-primary w-full justify-center" onClick={send}><Send size={14}/> Enviar</button></div><div className="card"><h3 className="section-title mb-3">Meus chamados</h3>{items.map(x=><div key={x.id} className="p-3 border-b border-dark-400"><p className="text-white">{x.code} · {x.subject}</p><p className="text-xs text-gray-500">{x.status} · {x.priority}</p></div>)}</div></div></div>}
