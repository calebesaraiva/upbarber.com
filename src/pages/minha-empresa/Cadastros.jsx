import{useEffect,useState}from'react';import{PageHeader}from'../../components/ui/PageHeader';import{settingsService}from'../../services/settings.service';export default function Cadastros(){const[items,setItems]=useState([]);useEffect(()=>{settingsService.listUsers().then(r=>setItems(r.data.data||[]))},[]);return <div><PageHeader title="Cadastros" subtitle="Usuários reais"/>{items.map(x=><div className="card mb-2" key={x.id}>{x.name} · {x.email} · {x.role}</div>)}</div>}
